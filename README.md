# pubmed-id-abstract-downloader

Given a CSV of search results from PubMed, enrich it with article abstract text.

## Usage

```bash
pip install -r requirements.txt
./downloader.py input.csv output.csv
```
